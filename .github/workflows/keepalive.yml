name: Keepalive

on:
  schedule:
    - cron: '0 9 1 * *'
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - uses: efrecon/gh-action-keepalive@main
